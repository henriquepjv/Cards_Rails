<h1>Cards Cadastrados</h1>

<% @cards.each do |card| %>
  <%= link_to card.portugues, card_path(card.id) %>
<!-- <%= card.portugues %> : <%= card.ingles %> -->
  <td><%= link_to 'Deletar', card, method: :delete, data: { confirm: 'Tem certeza?' } %>
  <br>
<% end %>

<br>
<br>

<%= link_to 'Novo card', new_card_path %>
